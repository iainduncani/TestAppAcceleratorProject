# TestAppAcceleratorProject

This project was generated by the [Liberty app accelerator](http://localhost:9082/start). To build the code clone the repository and run:

```
mvn install liberty:run-server
```